// STYELS
import { StyledNotFound } from "../styles/NotFound.styled";

export default function NotFound() {
  return (
    <StyledNotFound>
      <p>NOT ON EARTH... MAYBE ON ANOTHER PLANET !!</p>
    </StyledNotFound>
  );
}